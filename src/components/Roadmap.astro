---
// Roadmap - Styled as a hand-drawn treasure map on aged parchment paper
const items = [
  {
    title: 'TypeScript Support',
    description: 'TypeScript-first core, full IntelliSense, generic-typed hooks out of the box.',
    status: 'Done',
  },
  {
    title: 'React Wrapper',
    description: 'Released `@pip-it-up/react` with declarative PipWrapper and PipTrigger components.',
    status: 'Done',
  },
  {
    title: 'Persistent PiP across route transitions',
    description: 'Keep the floating PiP window alive and syncing state seamlessly while navigating between pages.',
    status: 'In Progress',
  },
];
---

<section id="roadmap" class="relative py-24 px-4 sm:px-6 overflow-hidden">
  <div class="max-w-3xl mx-auto">
    <!-- Section Header -->
    <div class="text-center mb-16" data-reveal>
      <h2 class="text-4xl sm:text-5xl font-handwritten font-extrabold mb-4 text-[var(--color-text)]">
        Roadmap
      </h2>
      <p class="text-base sm:text-lg font-typewriter text-[var(--color-text-muted)] max-w-xl mx-auto">
        Follow our dotted trail to the future of Picture-in-Picture.
      </p>
    </div>

    <!-- Parchment Treasure Map Card -->
    <div class="bg-[#ebdcb9] border-2 border-[#8c7a6b] p-8 sm:p-12 shadow-lg relative rounded-sm rotate-[0.5deg]" data-reveal>
      <!-- Washi tape at the top-middle -->
      <div class="absolute -top-4 left-1/2 -translate-x-1/2 washi-tape washi-tape-orange rotate-[-2deg] z-10">TREASURE MAP</div>
      
      <!-- Winding Wavy Trail (SVG) -->
      <div class="absolute inset-0 pointer-events-none opacity-20">
        <svg class="w-full h-full" viewBox="0 0 600 600" fill="none" stroke="#78350f" stroke-width="4" stroke-dasharray="10 10">
          <path d="M 300 40 Q 150 150 300 250 T 300 450 Q 450 550 300 580" />
        </svg>
      </div>

      <!-- Roadmap Items Stack inside a wobbly collage -->
      <div class="relative flex flex-col gap-8 font-handwritten max-w-xl mx-auto">
        {items.map((item, idx) => {
          const isEven = idx % 2 === 0;
          
          const isDone = item.status === 'Done';
          const isInProgress = item.status === 'In Progress';
          const badgeClass = isDone 
            ? 'border-emerald-600/40 bg-emerald-50 text-emerald-700'
            : isInProgress
            ? 'border-cyan-600/40 bg-cyan-50 text-cyan-700'
            : 'border-amber-600/40 bg-amber-50 text-amber-700';

          return (
            <div class:list={[
              'bg-white/85 border border-[#8c7a6b]/30 p-6 shadow-sm relative z-10',
              isEven ? 'rotate-[-1deg] translate-x-[-4px]' : 'rotate-[1deg] translate-x-[4px]'
            ]}>
              <div class="flex items-center justify-between gap-4 mb-2">
                <h3 class="text-2xl font-bold text-[#451a03]">{item.title}</h3>
                <span class={`text-sm font-bold px-2.5 py-0.5 border-2 rounded ${badgeClass}`}>
                  {item.status}
                </span>
              </div>
              <p class="text-sm font-sans text-amber-950/80 leading-relaxed">
                {item.description}
              </p>
            </div>
          );
        })}
      </div>
    </div>

    <!-- Issue Callout -->
    <div class="mt-16 text-center" data-reveal>
      <p class="text-base font-handwritten text-[var(--color-text-muted)]">
        Have a feature request or want to contribute? 
        <a 
          href="https://github.com/Shakya47/pip-it-up/issues" 
          target="_blank" 
          rel="noopener noreferrer" 
          class="text-[var(--color-text)] underline decoration-wavy hover:text-[var(--color-accent-dark)] font-bold ml-1"
        >
          Open an issue on GitHub &rarr;
        </a>
      </p>
    </div>
  </div>
</section>
